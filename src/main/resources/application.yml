spring:
  # ?????? ?? ??
  datasource:
    url: jdbc:mysql://localhost:3308/spring_db_project?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  # JPA ? Hibernate ??
  jpa:
    hibernate:
      # ddl-auto: ?????? ?? ? ???(???) ?? ??
      # none: ?? ??? ?? ?? (?? ?? ??)
      # create: ?? ??? ?? ? ?? ??
      # update: ??? ??? ??
      ddl-auto: none
    properties:
      hibernate:
        # MySQL 8 ??? ?? Dialect(??) ??
        dialect: org.hibernate.dialect.MySQL8Dialect
        # sql ??? ?? ??
        format_sql: true
        highlight_sql: true
  data:
    web:
      pageable:
        serialization-mode: direct
  flyway:
    enabled: true
    # classpath? resources ??
    locations: classpath:db/migration
# ?? ? ?? ??
server:
  port: 8080
  servlet:
    session:
      cookie:
        http-only: true

  # ?? ?? ??
logging:
  level:
    # Hibernate? ???? SQL ??? DEBUG ??? ??? ??
    org.hibernate.SQL: DEBUG