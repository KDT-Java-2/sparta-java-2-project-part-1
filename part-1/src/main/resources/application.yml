spring:
  # ?????? ?? ??
  datasource:
    url: jdbc:mysql://localhost:3307/sparta_project?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA ? Hibernate ??
  jpa:
    hibernate:
      # ddl-auto: ?????? ?? ? ???(???) ?? ??
      # none: ?? ??? ?? ?? (?? ?? ??)
      # create: ?? ??? ?? ? ?? ??
      # update: ??? ??? ??
      ddl-auto: none
    properties:
      hibernate:
        # MySQL 8 ??? ?? Dialect(??) ??
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        highlight_sql: true

# ?? ?? ??
logging:
  level:
    # Hibernate? ???? SQL ??? DEBUG ??? ??? ??
    org.hibernate.SQL: DEBUG

  # flyway
  flyway:
    enabled: true
    locations: classpath:db/migration # sql ??? ???? ??? ??, classpath:? resources ???? ???

# Swagger UI ?? (Springdoc)
springdoc:
  swagger-ui:
    enabled: true # Default? true?? ????? ?? ??? ???? ??? false? ?????

# ?? ? ?? ??
server:
  port: 8080

